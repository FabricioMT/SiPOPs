version: '3.8'

services:
  db:
    image: pgvector/pgvector:pg15
    container_name: sipops-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: sipops
      POSTGRES_PASSWORD: sipops_password
      POSTGRES_DB: sipops
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-extensions.sql:/docker-entrypoint-initdb.d/init-extensions.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sipops"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - sipops-network

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sipops-api
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgresql+asyncpg://sipops:sipops_password@db:5432/sipops
      - SECRET_KEY=your-super-secret-key-change-in-production
      - ACCESS_TOKEN_EXPIRE_MINUTES=30
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - sipops-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: sipops-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - api
    networks:
      - sipops-network

volumes:
  postgres_data:

networks:
  sipops-network:
    driver: bridge
